# Copy this file to .env and fill the values before running the server
# MongoDB Atlas connection string (get from Atlas UI)
MONGODB_URI="mongodb+srv://student_mngmt:Student_mngmt@smgs.h4scbnl.mongodb.net/?appName=Smgs"
MONGODB_DB="smgs"

# JWT secret for signing tokens (replace with a strong secret)
JWT_SECRET="student_mngmt_sudharshan_secret"

# Optional: server port
# PORT=4000
